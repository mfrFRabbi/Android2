<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".ScrollTab"></activity>
        <activity android:name=".HtmlContentInTextView" />
        <activity android:name=".HtmlWebView" />
        <activity
            android:name=".FragmentDemo"
            android:screenOrientation="landscape" />
        <activity android:name=".ExpandableListViewActivity" />
        <activity android:name=".AutoCompleteText" />
        <activity android:name=".FeedBackActivity" />
        <activity android:name=".ProgressActivity" />
        <activity android:name=".SpinnerActivity" />
        <activity
            android:name=".ActivityList"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>